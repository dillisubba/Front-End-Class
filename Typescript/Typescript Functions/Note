Typescript Function
------------------------------
These are the primary blocks of any program.
These are the fundamental building block of any application.

It makes the code reusable, readable and maintainable.

Advantages
------------------
code reusability: we can call function multiple times.
less coding : we write less lines of code in functions.
easy to debug: we can easily identify the error.

create a function
------------------------
A function declaration tells the compiler about function name, parameters and return type.
function definition contains actual statements which are going to be executed.

syntax:
-----------
function functionname(parameters)
{   
//statements;
}

function call
--------------------
we can call a funciton from anywhere in the program.
syntax:
-----------
functionname(with or without parameters);

Named Function
-------------------------
A function which consists of a name is known as Named function.

function display()
{
console.log("welcome to named function");
}

display();


//create a function without any parameter
function display()
{
console.log("named function");
}

//call function
display();


//function with one parameter
function show(fname:string)
{
console.log("Name is "+fname);
}
show("srinivas");

//function with two parameters
function Add(num1:number, num2:number)
{
console.log("total is "+(num1+num2));
}
Add(45,65);

//function with parameters and with return type.

function AddNums(num1:number, num2:number)
{
return num1+num2;
}
console.log(AddNums(5,6));
-----------